# Trigger when commits are pushed to the master branch
on:
  push:
    branches:
      - develop
  pull_request:
  workflow_dispatch:


jobs:
  terraform-actions:
    name: Workflow
    runs-on: circleci/golang:1.16
    steps:

    - name: Checkout Repository
      uses: actions/checkout@master